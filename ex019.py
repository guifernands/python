chave = 'gladiador'
palavra = ('*') * len(chave)

while True:
    tentativa = input('Digite uma letra: ')

    if len(tentativa) > 1:
        print('Digite apenas uma letra.')
        continue
    
    if tentativa in chave:
        if tentativa == g:
            palavra = 
    else:
        print(palavra)
        continue